<main>
    <h1>Welcome to Jukebox Database</h1>
    {{#each artist}}
    <div class="artistNames">
        <a href="/artist/{{_id}}">{{artistName}}</a>
        {{ content }}
    </div>
    {{/each}}
</main>