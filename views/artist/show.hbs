<main>
    <h1>Artist<br>you are<br>viewing:</h1>

    <div class="screen">

        <div class="artist-container-show"></div>

        <h2 class="artistName">{{artist.artistName}}</h2>

        <h4 class="artistDescription">{{artist.artistDescription}}</h4>

        <h4>Click on album to see songs:</h4>
        {{#each artist.artistAlbum}}
        <a href="/album/{{id}}">{{albumName}}</a>
        <h4 class="albumDescription">{{albumDescription}}</h4>
        {{/each}}

        <h4>Artist Genre: {{artist.artistGenre}}</h4>
        <a href={{artist.artistLink}} class="artistLink">Band's Website or Info</a>

        <div class="artistButtons">
            <a href="/add"><button>Add an album</button></a>

            <a href="/artist/{{artist._id}}/edit"><button>Edit artist info.</button></a>

            <form action="/artist/{{artist._id}}?_method=DELETE" method="POST" class="artistDelete">
                <button type="submit">Delete this artist</button>
            </form>
        </div>

        <a href="/">Return to Home Page</a>

    </div>

</main>