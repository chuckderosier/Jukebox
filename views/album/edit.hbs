<main>

	<div class="form">

		<h1 class="formTitle">Edit<br>the<br>album</h1>

		<form action="/album/{{album._id}}?_method=PATCH" method="POST">
			<p class="input-field">Edit album name:</p>
				<input type="text" name="albumName" value="{{album.albumName}}">
			<p class="input-field">Edit album description:</p>
				<input type="text" name="albumDescription" value="{{album.albumDescription}}">
			<p class="input-field">Edit the release year:</p>
				<input type="number" name="albumYear" value="{{album.albumYear}}">
			<button type="submit" class="submitButton" >Save your edits</button>
		</form>

		<a href="/">Return to Home Page</a>

	</div>
</main>